<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil Utilizator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="card shadow-lg p-4 mx-auto" style="max-width: 500px;">
            <h2 class="text-center text-primary">Profilul Meu</h2>
            <p class="text-center"><strong>Utilizator:</strong> <%= user.username %></p>
            <form action="/profile/update" method="POST">
                <div class="mb-3">
                    <label for="description" class="form-label">Descriere:</label>
                    <textarea class="form-control" id="description" name="description" rows="3"><%= user.description || '' %></textarea>
                </div>
                <button type="submit" class="btn btn-success w-100">Salvează</button>
            </form>
            <% if (!user.is_candidate) { %>
                <form action="/profile/candidacy" method="POST">
                    <button type="submit" class="btn btn-warning w-100 mt-3">Candidez la alegeri</button>
                </form>
            <% } else { %>
                <p class="text-center text-success"><strong>Ai candidat la alegeri!</strong></p>
            <% } %>            

            <% if (errorMessage) { %>
                <div class="alert alert-danger mt-3 text-center"><%= errorMessage %></div>
            <% } %>
            <hr>
            <a href="/" class="btn btn-secondary w-100">Înapoi</a>
        </div>
    </div>
</body>
</html>